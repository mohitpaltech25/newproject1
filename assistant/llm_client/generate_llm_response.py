from typing import Iterable

# Minimal stub for LLM streaming

def generate_llm_response(prompt: str) -> Iterable[str]:
    # Replace with real client call; yield sentence-like chunks
    yield "I'm a placeholder response to: " + prompt
